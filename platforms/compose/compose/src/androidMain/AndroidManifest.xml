<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Android 11+ filters PackageManager queries unless the consuming app declares the intent it
      needs to discover. Compose Foundation builds PROCESS_TEXT menu items from queryIntentActivities,
      so keep this query in the Tiqian Android artifact rather than requiring every host to repeat it.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
