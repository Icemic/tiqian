node_modules/
dist/
.parcel-cache/
# Build products emitted next to the .ts sources (G1 in-place build). Never
# tracked: the package scripts regenerate them (pretest runs tsc).
*.js
*.d.ts
*.tsbuildinfo
